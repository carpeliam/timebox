---
platform: linux

image_resource:
  type: docker-image
  source: {repository: mwallasch/docker-ruby-node}

inputs:
- name: timebox-src

outputs:
- name: timebox-installed

run:
  path: timebox-src/concourse/npm-install.sh
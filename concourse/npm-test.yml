---
platform: linux

image_resource:
  type: docker-image
  source: {repository: mwallasch/docker-ruby-node}

inputs:
- name: timebox-src
- name: timebox-installed

run:
  path: timebox-src/concourse/npm-test.sh